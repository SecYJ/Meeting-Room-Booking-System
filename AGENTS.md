# Project Rules

1. This project uses Bun as the runtime and package manager.

# TypeScript Rules

1. Do not put explicit return type, prefer auto infer instead.
2. Use type over interface.
3. Use arrow function instead of function declaration.
